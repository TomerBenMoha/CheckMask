# Purim Chess (web)

Run the game locally: from this folder, start a static server, then open the URL in a browser.

Example:

```bash
python -m http.server 8080
```

Then open **http://localhost:8080**.
