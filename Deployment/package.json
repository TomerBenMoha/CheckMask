{
  "name": "purim-chess",
  "version": "1.0.0",
  "description": "A Purim-themed chess variant where pieces wear costumes and Kings absorb powers",
  "private": true,
  "scripts": {
    "cap:init": "npx cap add android && npx cap sync",
    "cap:sync": "npx cap sync",
    "build:android": "cd android && ./gradlew assembleDebug"
  },
  "dependencies": {
    "@capacitor/core": "^6.0.0",
    "@capacitor/android": "^6.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.0.0"
  }
}
